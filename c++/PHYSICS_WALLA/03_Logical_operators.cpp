#include<bits/stdc++.h>
using namespace std;

int main(){
    int a=10;
    int b=20;
    cout<<(a&&b)<<endl;//logical and
    cout<<(a||b)<<endl;//logical or
    cout<<(a^b)<<endl;//logical xor
    cout<<(!a)<<endl;//logical not
    return 0;
}